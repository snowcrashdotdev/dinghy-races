{% extends 'base.html.twig' %}

{% block title %}Permanent Leaderboards{% endblock %}

{% block body %}
    <header>
        <h1>High Scores for {{website.name}} Games, Past &amp; Present</a>
    </header>
    <main>
    {% for game in games %}
        {% set href = path('pb_show', {game: game.name}) %}
        <p><a href="{{href}}">{{game.name}}</a></p>
    {% endfor %}
    </main>
{% endblock %}
