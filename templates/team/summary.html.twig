<article class="summary summary-team bg-white flex-column margin">
    <h3 class="bg-primary padding"><a href="{{ path( 'team_show', {'team': team.id} ) }}">{{ team.name }}</a></h3>
    <ul class="list-style-none">
        {% for member in team.members %}
            {% set href = path('profile_show', {
                'username': member
            }) %}
            <li>
                <a href="{{href}}">{{member.username}}</a>
                {% if member.profile.hasTwitch %}
                    <a class="icon" target="_blank" href="{{member.profile.social}}" class="icon">{{ source('svg/twitch.svg')|raw }}</a>
                {% endif %}
            </li>
        {% else %}
            <li>No team members registered</li>
        {% endfor %}
    </ul>
</article>