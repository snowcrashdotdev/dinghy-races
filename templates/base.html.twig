<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <meta http-equiv="robots" content="[noindex|nofollow]">
        <meta name="description" content="Competitive, team-based video game tournaments.">
        <title>{{appname}} - {% block title %}{{appdescription}}{% endblock %}</title>
        {% block stylesheets %}<link rel="stylesheet" media="screen" href="{{ asset('/css/dist/style.min.css') }}">{% endblock %}
    </head>
    <body>
        {% include '_partials/flash.html.twig'  %}
        {% include '_partials/navigation.html.twig' %}
        {% block body %}{% endblock %}
        {% block sidebar %}{% endblock %}
        {% block footer %}{% endblock %}
        {% block javascripts %}
        <script>
        // Remove flash messages after they serve their purpose
        document.addEventListener('DOMContentLoaded', function(){
            var flashMessages = document.getElementsByClassName('flash')
            setTimeout(function(){
                for (var i = 0; i < flashMessages.length; i++) {
                    var flash = flashMessages.item(i)
                    flash.parentNode.removeChild(flash)
                }
            }, 12000)
        })
        </script>
        {% endblock %}
    </body>
</html>
